TSTTB_DIR = ../TSTTB
TSTTG_DIR = ${HOME}/tstt/cvs/TSTTG
CGM_DIR = ${HOME}/tstt/cvs/cgm
CUBIT_BASE_DIR = ${CGM_DIR}

include ${TSTTB_DIR}/TSTTB-Defs.inc

include ${CGM_DIR}/util/OtherFiles/CUBIT.lin
include ${CGM_DIR}/util/OtherFiles/make.client
include ${CGM_DIR}/util/OtherFiles/cubit.myown

PREFIX=

TSTTG_SERVER_INCLUDES = ${CGM_INCLUDES}  -I${TSTTG_DIR} -I${TSTTG_DIR}/server ${TSTTB_SERVER_INCLUDES}
TSTTG_SERVER_LIBS = -L${TSTTG_DIR} -lCGMserver -Wl,-rpath,${TSTTG_DIR} ${TSTTG_IMPLEMENTATION_LIBS} ${TSTTB_SERVER_LIBS}


GEOM_LIBS_LINK := ${GEOM_LIBS_LINK:-Lutil%=-L${CGM_DIR}/util%}
GEOM_LIBS_LINK := ${GEOM_LIBS_LINK:-Lgeom%=-L${CGM_DIR}/geom%}
EXTRA_GEOM_LIBS_LINK := ${EXTRA_GEOM_LIBS_LINK:-Lgeom%=-L${CGM_DIR}/geom%}
CGM_LIBS := ${CGM_LIBS:geom/%=${CGM_DIR}/geom/%}
EXTRA_GEOM_LIBS := ${EXTRA_GEOM_LIBS:geom/%=${CGM_DIR}/geom/%}

TSTTG_SERVER_FILES = ${TSTTG_DIR}/libCGMserver.so ${CGM_LIBS} ${EXTRA_GEOM_LIBS}
TSTTG_IMPLEMENTATION_LIBS = ${GEOM_LIBS_LINK} ${EXTRA_GEOM_LIBS_LINK} ${GEOM_LIBS_LINK} 
TSTTG_NAMESPACE = TSTTG_CGM
TSTTG_CLASS = CgmGeom

TSTTG_CLIENT_C_INCLUDES = -I${TSTTG_DIR}/Cclient 
TSTTG_CLIENT_C_FILES = libCGMCclient.so
TSTTG_CLIENT_C_LIBS = -L${TSTTG_DIR} -lCGMCclient
